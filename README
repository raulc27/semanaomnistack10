Coleção de aplicativos desenvolvidos durante a 10º Semana Omnistack, contendo:

1) Backend em NodeJS (api)
2) App WEB em ReactJS
3) App mobile (celular) em React Native (Expo)

Acompanhando os vídeos e "codando" direto... 

Criando modificações, elas "sofrerão" update aqui.

Após ter baixado ou clonado o projeto, entre em cada diretório e:

<code>
<>$ npm install
</code>

Para rodar o backend: <code>$ yarn dev</code>
Para rodar o mobile: <code>$ expo start</start>
Para rodar o web: <code>$ yarn start</code>


Até!

Contato para projetos:

Skype: raulc27